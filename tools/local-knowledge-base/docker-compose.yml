services:
  local-knowledge-base:
    build: .
    container_name: page-agent-local-kb
    ports:
      - "${KB_PORT:-8787}:8787"
    environment:
      KB_DB_PATH: /data/knowledge.sqlite3
      KB_API_KEY: ${KB_API_KEY:-}
    volumes:
      - ./data:/data
    restart: unless-stopped
